{
  "name": "test",
  "version": "1.0.0",
  "description": "note: the `patches` subfolder is used as the build folder, so that js files used by node.script boxes are in the same folder as the patch.",
  "main": "index.js",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^14.18.36",
    "@types/uuid": "^9.0.1",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/geofholbrook/n4m-template.git"
  },
  "bugs": {
    "url": "https://github.com/geofholbrook/n4m-template/issues"
  },
  "homepage": "https://github.com/geofholbrook/n4m-template#readme",
  "bin": {
    "n4m-template": "bin/index.ts"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.test.ts"
    ]
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
